# local-llm-workbench-windows-amd-openwebui-ollama
Local LLM Workbench (Windows + AMD) — One-click installer + desktop app that runs Open WebUI + Ollama in-app (Electron) and ships a preconfigured filesystem OpenAPI tool (no manual setup). Includes admin-only account setup, service health dashboard, model pull UI, and Windows-friendly troubleshooting.
